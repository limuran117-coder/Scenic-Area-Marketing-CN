# 2026-08-12 全面 cron 修复优化

站长要求：全面检查所有 cron，优化方法/途径减少报错。

## 诊断结论（数据支撑 cron_run_logs 1577条）
- **近14天 error 分布**：08:00 13次 / 19:00 9次 / 09:00 9次 / 10:00 7次 → **上午8-10点 + 晚19-21点是模型限流重灾区（deepseek-v4-flash）**
- **error 类型**：RATE_LIMIT 40次（主导）、EXEC_FAIL 11次、PROCESS_FAIL 2次
- **逐日**：8/6仅1次，8/7起每天10+次 → 8/7起模型侧系统性恶化

## 已执行的修复
1. **舆情数据源清单.md SSOT 路径刷新** ✅
   - 客流主源 `~/Desktop/2026游客量统计.csv` 旧路径 → 更新为 `~/Downloads/2026游客量统计 (N).csv`，标注 6/23 迁移 + 文件名带括号禁止裸 find

2. **周度客流洞察（ea3cc4ec）prompt 加固 + timeout 1500** ✅
   - 根治根因：禁止在 Downloads 裸 find「游客|数量」，改用 weekly_visitor_report.py 的 find_latest_ssot() 脚本定位
   - timeout 从 0(无超时) → 1500s

3. **抖音指数日报（a23a8099）timeout 900→1500** ✅
   - 连续 4 次 900s 撞顶（model-call-started），提高缓冲

4. **竞品爆款拆解（b8fb57da）timeout 900→1500** ✅ (errN=5的历史，模型限流主导)

5. **竞品关键词深度分析（9bf47f42）timeout 900→1500** ✅

6. **周日洞察汇总（f79eaffc）timeout 1500**（确认已有）
7. **周末市场观察（956f3659）timeout 900→1500** ✅

8. **删除过期任务**：deepseek统一观察-8/11回顾（c1918d26，nextRun排到2027）✅

9. **孤儿任务 disabled**：周度竞争格局报告（31080d06）功能已合并进周日洞察汇总，仍enabled → disabled ✅

## 🧬 新增：Cron 自进化治理机制（站长诉求 10:45）

**设计**：纯 Python 数据层脚本 + 双 cron 层次，核心不引 LLM（遵守 Token 守则）

**脚本**：`scripts/cron_governor.py`
- --daily 每日哨兵：近 N 天异常任务（连续err/timeout撞顶/高错误率）+ 限流高峰识别
- --weekly 每周治理：深度健康报告 + 进化建议 JSON（写 scripts/.cron_governor/report_*d.json）
- --apply 自动应用安全优化（--dry-run预览）：提高 timeout / 禁孤儿；绝不删任务/改时段
- 轻量探针白名单（CDP健康探针/Ontology自检/知识进化）不参与提超时；delete_after_run一次性排除
- 已用：周二客流深度报告 timeout None→1500（写库成功）

**包装**：`scripts/cron_governor_daily.sh`（纯脚本静默采集，不发群）

**双 cron**：
- 每日哨兵(0c6f51f7) 10:30 静默采集
- 每周治理(8ad42b37) 周日20:00 自动apply + 进化报告发卡（≤1张，7/29铁律）

**⚠️ 教训（测试误发群）**：10:46 测试 daily 误发 2 张测试卡到电影小镇群，已撤回（DELETE /im/v1/messages）。教训：new 脚本测试前确保无发卡逻辑或先隔离。

## 不需要修的
- web_search provider=searxng 正常可用（@测试通过）
- send_feishu_card.py 语法/逻辑健全（有quality gate + token刷新重试）
- 抖音采集脚本没坏（4次都是模型层超时，非采集层）

## 待观察
- 抖音指数日报明天8:30自然跑验证（不改，等自然触发）
- 竞品爆款/关键词已通过timeout缓解，观察近期
