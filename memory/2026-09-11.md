
## 舆情监控 cron（07:30）
- 公开检索断链第 5 天：`web_search`(searxng) 4 个查询全部 0 条；搜狗/百度验证码拦截，必应 query 改写返回无关结果
- 绕过路径：360 搜索（so.com）curl + UA 可拿到存量结果块，但无时间过滤、无近 14 日窗口
- 可确认存量：负面=「69元门票/节目少」避雷吐槽（最新样本 2025-02）；正面=官方「中秋赏月7小时」夜游 IP（2024 有据，2026 春节口碑正向）
- 2026 中秋活动公开检索未见新增确认 → 与我方产品上架进度互相印证
- 结论：🟡 需关注（低信度），**不落健康/危机硬结论**，如实上报
- 卡片已发：om_x100b6507aa1778a4c254235d70f4761
- 已追加 `wiki/行业知识/结论索引.md`（9/11 区块，exec cat >> 非 edit）

## CDP健康探针（cron 3eacd6bb）07:41
- 探针：curl /json/version → **200**（端口健康，Chrome PID 50345 存活，窗口未关）
- 发现：`scripts/cdp_restore_tabs.py` 旧版（Playwright `connect_over_cdp`）在 **Chrome 152** 上 `<ws preparing>` 挂起超时 → 标签恢复永远失败
- 深挖：HTTP `PUT /json/new` 在 Chrome 152 上也直接挂死（即使 about:blank），且会**阻塞整个 CDP HTTP 服务**（后续 /json/version 也变 000，约 45s 后自愈）
- 修复：重写 `cdp_restore_tabs.py` 为 **CDP WebSocket + `Target.createTarget`**（websockets 16.0，无 Playwright 依赖；`Target.createTarget` 不等待页面加载，立即返回）
- 结果：7 个固定标签全部在位（Tab0-Tab6 ✅）
- ⚠️ 遗留：脚本仍先调 `/json/version`（HTTP）；若 HTTP 被 hung 请求阻塞会误报"不可达"。后续可改为 WS 直连或 HTTP 短超时+WS 兜底
