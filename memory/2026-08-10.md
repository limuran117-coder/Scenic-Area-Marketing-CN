# 2026-08-10 工作日

## 系统模型统一 deepseek（09:00 完成）
- **站长要求**：整个系统只使用 `deepseek/deepseek-v4-flash`，不再用其他模型
- **执行**：
  - 清空全局 fallback `[minimax/MiniMax-M2.7]` → `[]`
  - 移除 models registry 里 MiniMax-M3/M2.7 别名（仅留 DeepSeek）
  - **移除 minimax provider**（models.providers 全库仅剩 deepseek）
  - cron 35 个任务全部显式 `deepseek-v4-flash`（补了 Obsidian客流数据同步 1 个）
  - 重启 Gateway 生效
- **备份**：`~/.openclaw/openclaw.json.bak-20260810`（移除 minimax 前）

## 排查发现（error 根因分层）
1. 旧大批 error（MiniMax rate-limited ~100次）→ fallback 到 MiniMax-M2.7 被限流 → **已根除**
2. 6月存量 error（Token配额耗尽/401旧key）→ 历史遗留
3. 重启残留 error（job interrupted by gateway restart）→ 正常
4. deepseek 偶发限流/超时（model-call-started timeout）→ 高峰瞬时限流

## 验证
- deepseek API 直连：HTTP 200，2.2s 响应，正常
- CDP Cookie 采集：抖音/小红书/微博 cookie 全有效，CDP 18800 健康
- 手动 `cron run force` 2 次未落库（重启排队期，非真实问题）

## 🔭 观察中（站长要求观察一天）
- 明天（8/11）回顾：cron error 是否因移除 MiniMax 而大幅下降
- 关注点：重任务（竞品关键词/CDP同步）在 deepseek 高峰限流时是否偶发超时
- 待办：8/11 对比 cron_run_logs 的 error 分布

## 🔧 11:00 深度排查 added
- **发现web_search坏**：tools.web.search.provider=minimax，但 minimax Token Plan 用尽 → web_search 报"已达到 Token Plan 用量上限"。这是独立于模型的问题。
- **发现cron deepseek超时根因**：带显式timeout的cron对云模型强制60s流stall上限；deepseek推理慢启动(>60s首token)被掐 → model-call-started超时。
- **动作**：CDP健康探针 timeoutSeconds 300→600（用cron update工具，直接改SQLite会被Gateway内存态覆盖！）
- **教训**：改cron必须用 cron action=update，不能直接UPDATE SQLite。
- **待决**：33个cron带显式timeout都受60s idle影响，是否统一调大？需观察CDP探针600s版是否跑通。

## 🔍 10:45-11:00 全面系统体检（站长要求：冲突/错误/冗余/漏洞/配置/失效全查）

### 已修复 ✅
1. **CDP健康探针 timeout 300→600**（cron update 工具，实测 10:32 跑通 ok）
2. **CDP Cookie同步 timeout 120→900**（08:36 model-call-started 超时修复）
3. **其余 11 个 ≤600s cron timeout 统一调至 900s**（每日复盘投递/tmp清理/Cookie健康/飞书审计/电影小镇营销日历/爆款公式/GitHub高星标/系统进化/月度复盘/每日复盘生成/周末市场观察）
4. **调度冲突修复**：周度技能探索 10:00→10:30（避开周末市场观察）；周日洞察汇总 11:00→11:30
5. **冗余任务禁用**：系统进化审视 + Wiki健康检查（已被周日洞察汇总合并，message 里写明"已disabled"但实际 enabled，漏网之鱼）
6. **观察 cron 补 model=deepseek**（原 NULL）

### 发现但需站长决策 ⚠️
1. **web_search 全局失效**（10:50 站长决定彻底弃用 minimax，见下方清理记录）：① minimax 搜索 Token 耗尽；② duckduckgo 国内网络不通（HTTP 000）；③ 已按站长要求彻底删除 minimax 全部配置→ web.search 暂禁用 → 影响 4 个 cron（舆情监控/竞品内容动态/文旅营销案例/周末市场观察），**需站长提供替代搜索方案**
2. **21 个 blocked TaskFlow 孤儿记录**（历史竞品采集任务镜像，不影响生产，无清理命令，低优先级）
3. **pixverse 插件 deprecation 警告**（providerAuthEnvVars 废弃，非紧急）
4. **scripts/ontology/ontology_daily_work.py 缺失**（对应 cron 已 disabled，非阻塞）

### 健康项 ✅
- deepseek API: HTTP 200 (0.2s)
- 飞书 token: 获取成功
- Cookie: 抖音61/小红书21/微博24 全有效
- 所有核心脚本语法 OK
- 端口 18789/18800/7897/37777 全 LISTEN
- 磁盘 19%、负载正常

## 🧹 10:50-11:00 minimax 彻底清除（站长决定：minimax 不再使用）

### 清理清单（全部完成 ✅）
1. **openclaw.json**：删 plugins.entries.minimax + allow数组minimax + auth.profiles minimax:default/cn（0残留）
2. **~/.openclaw/.env**：删 OPENCLAW_MINIMAX_PROVIDER_APIKEY + WEBSEARCH_APIKEY + 注释行（0残留）
3. **~/.bashrc**：删 MINIMAX_API_KEY（0残留）
4. **~/.openclaw/agents/main/agent/plugins/minimax/**：整目录移废纸篓
5. **cron_jobs**：模型/名称层 0 残留（观察任务 message 文本已改为"deepseek-only 时代"）
6. **TOOLS.md / SKILL-USAGE-GUIDE.md**：更新过时 minimax 引用
7. **备份保留**：openclaw.json.bak-20260810-providerminimax（移除瞬间快照，审计凭证）

### ⚠️ web_search 现状（需站长决策）
- minimax 搜索后端已删 + duckduckgo 网络不通（HTTP 000）→ **web_search 已禁用**（enabled:false, provider:duckduckgo 占位）
- 影响 4 个 cron：舆情监控/竞品内容动态/文旅营销案例/周末市场观察（依赖 web_search）
- 需站长提供替代搜索方案（如 kimi/perplexity/brave key，或确认可用的国内搜索后端）

### 系统健康
- gateway 重启成功（pid 4905）
- providers=['deepseek']，auth=['deepseek:default']，全系统唯一模型 deepseek-v4-flash

## 🌐 11:00-11:08 web_search 替代方案落地（searxng 自托管）

### 站长决策链路
1. 站长要求"替代方案都用 DeepSeek"，给了新 key `sk-653…cca5`
2. **查证**：OpenClaw 无 deepseek 搜索 provider，DeepSeek API 是纯 LLM 无 web_search 接口 → 方案不可行
3. 站长要求"去 GitHub 搜索可替代解决方案"
4. 站长新 key 已替换进 .env（DEEPSEEK_API_KEY，原 sk-db2b 已弃用）✅

### GitHub 调研发现的候选
| 方案 | 需要 | 结论 |
|---|---|---|
| exa-deepseek-chat | EXA_API_KEY(付费) | 需额外key |
| baidu-search-plugin | 百度千帆 bce-v3 key | 需申请，付费 |
| idefav/camofox web-search | 自托管浏览器+bing | 重，底层仍依赖搜索引擎 |
| **searxng 自托管** ✅ | docker + 无key | **选定，落地成功** |
| kimi/moonshot | Moonshot key(付费) | 备选 |

### 关键网络实测（这台 Mac mini）
- bing: 可达(302) | 百度: 可达但触发安全验证 | google: 不通(000) | duckduckgo: 不通
- → 裸 curl 抓搜索会被反爬，需专门搜索服务

### ✅ searxng 落地（成功）
- 启动 Docker Desktop（原未运行）
- `docker pull searxng/searxng:latest` 成功
- 配置 `~/.openclaw/searxng/settings.yml`：只启用 bing/bing news/baidu 引擎，禁 google/ddg/brave
- 运行容器：`-p 8888:8080`，restart=unless-stopped
- `openclaw plugins install @openclaw/searxng-plugin` 成功
- 配置 `tools.web.search.provider='searxng'` + enabled + baseUrl=127.0.0.1:8888
- 重启 gateway（pid 5573）
- **端到端验证**：web_search("郑州电影小镇 游玩攻略") → provider=searxng，返回携程/Trip 相关结果 ✅

### ⚠️ 已知质量问题（待优化）
- bing 引擎偶尔返回无关结果（如第一条日本网站）
- 结果有 `<<<EXTERNAL_UNTRUSTED_CONTENT>>>` 包装 = OpenClaw 标准安全标记（正常）
- 后续可调 searxng categories/语言/引擎权重减少噪音

### 系统状态
- providers=['deepseek'] 唯一；auth=['deepseek:default']；模型 deepseek-v4-flash
- minimax 已彻底清除（0残留）；web_search 用 searxng 恢复
- 依赖 web_search 的 4 个 cron（舆情/竞品动态/文旅案例/周末观察）已恢复能力

## 🔍 11:09 站长系统级验收核查（cron/知识库/进化闭环/配置冗余）

### CRON 真实健康度（重要修正）
**推翻之前误判**：8/7-8/9 的"全失败"不是 deepseek 限流，而是 **MiniMax-M2.7 fallback 失败**（此时 fallback 链让所有任务走 MiniMax，而 MiniMax Token 早耗尽）：
- 8/7: MiniMax 6/6 失败 | 8/8: 10/10 | 8/9: 12/12
- 8/10 移除 MiniMax+fallback 后：deepseek-v4-flash 5 运行 3 成功（10:35后 CDP探针/周日洞察/周度技能全 ok）
- **当前 29 个启用 cron 全部显式 deepseek，非deepseek=0** ✅

### 知识库（真实且丰富）
- 结论索引：1258行/1077条，8月新增96条，8/10周日洞察W33刚写入
- 案例库 118 个（全国景区），竞品分析/系统/行业知识/电影小镇各子库齐全
- 有"结论矛盾检查"机制（新结论先读旧索引）

### ⚠️ 进化闭环缺口（诚实报告）
- **竞品分析产出断档 6 周**：wiki/竞品分析/竞品动态追踪最后更新 6/30（W27）→ 7-8月因 MiniMax 持续失败中断
- **小红书日报断档 49 天**（站长决定不修）
- 但 8/10 系统恢复 deepseek 后，周日洞察 W33 已重新产出（证明闭环可恢复）
- **R08 执行层缺口**仍是核心问题：系统持续预警"8月静默浪费暑期"，但执行层（人）未落地

### 配置冗余（待优化，未擅动）
- duckduckgo 插件 disabled 但配置残留（可清理）
- 多个默认 preset 插件（google-meet/copilot/lobster/openshell/voice-call/tokenjuice等）电影小镇业务用不到，但关闭风险>收益，未动
- 7 个禁用 cron 均合理（合并/断档/ontology）

## 🏛️ 11:13 知识库+记忆架构深查（发现5类问题）

### 知识库架构
| 问题 | 详情 | 处置 |
|---|---|---|
| index.md 漂移 | 导航入口最后更新6/30，但内容长到8/10 | 需更新索引 |
| 51 orphan | 内容未在index引用（部分是历史归档合理） | 分类处理，非全修 |
| 8 contradiction | **lint误报**（年度153万全一致，log.md已确认） | 不修 |
| 案例库 | 118个案例，8/5/8/6有新增未索引 | 补索引 |

### 记忆架构
| 问题 | 详情 | 处置 |
|---|---|---|
| LEARNINGS断裂 | TOPICS-INDEX引用.learnings/LEARNINGS.md但文件不存在 | 建文件或改引用 |
| SESSION-STATE旧 | 停5/15，未随更新 | 更新或归档 |
| MEMORY.md | 99行，健康(上限100) | 正常 |
| 日粒度 | 100个日期文件，连续 | 正常 |

### 竞品断档（待站长确认补法）
- 竞品周报停W27(6/30)，关键词池实质停4/22
- 已6周无产出 → 需确认：补历史7周 vs 合并回顾 vs 本周起恢复

## 📊 13:38 竞品补课 B+C 执行
- **B 积压回顾**：spawn 子任务 competitor_backfill_review 生成 `2026-W28-W33-竞品格局积压回顾.md`（素材=结论索引8月竞品结论 + web_search补充 + W27格式参考）
- **C 周报恢复**：周度竞争格局报告 cron 31080d06 重新启用 ✅（原 disabled 是断档根因）→ 8/17(周日)10:00 恢复跑 W34
- 周报 payload 完整：洞察4问 + 飞书卡片 + 结论索引写入 + 验证记录闭环，deepseek/900s

## 🏗️ 13:44 【重大方向】站长拍板：将系统建成 ontology 架构（长期不限时工程）

站长要求按自己理解持续推进，不懂就搜索资料。这是系统架构级方向转变。

### 现状基线（13:44 核查）
**双 ontology 体系并存：**
1. **业务 ontology**（scripts/ontology_constants.py v1.0 + ontology_store.py + 生产库 .profile/ontology/ontology_store.db）
   - 针对景区营销建模：SCENIC_SPOT_MAP(10景)、metric_snapshots(100条)、tourist_segments(5)、content_assets(28)、spot_relations(10)
   - OntologyStore = 单表 upsert + 快照 + 统计（20函数），OntologyQuery(14函数)
   - **能力局限**：无关系遍历/无图谱查询/无LLM层/无ActionDispatcher
2. **通用 ontology skill**（skills/ontology/，clawhub安装）— Person/Project/Task/Document 通用图谱

**关键事实**：
- 生产库最后写入 7/22，停 18 天
- 两个 ontology cron 均 disabled
- W5 规划的 QueryHandler/LLM Translator/ActionDispatcher/Schema注入 **未实现**（代码 grep 不到）
- 空壳库 scripts/ontology.db、scripts/ontology/ontology_store.db 均 0 字节（残留）

### 下一步（待规划细化）
目标：ontology 成为系统真正的骨架（知识储存+关系查询+智能层+行动计划）
需要：确立单一路径（业务 vs 通用skill vs 融合）→ 恢复活跃 → 建设关系/图谱层 → LLM 智能层 → 接入日常流水

## ✅ Ontology 工程 M1+M2 完成（13:46-13:52）
- **M1 ADR**：wiki/技术配置/Ontology架构设计/ADR-001-ontology-架构融合路径.md
  - 决策：融合通用图谱引擎(skills/ontology) + 景区领域模型 + 保留生产库SQLite
- **M2 领域 schema**：wiki/技术配置/Ontology架构设计/movie-town-schema.yaml v0.1.0
  - 5 types(ScenicSpot/TouristSegment/MetricSnapshot/ContentAsset/MarketingCampaign)
  - 6 relations(competes_with/located_in/has_metric/targets_segment/published_by/linked_to)
  - 2 constraints
- **端到端验证通过**：
  - schema 被引擎正确解析(types包裹结构，⚠️ 发现 references/schema.md 平铺写法与引擎实际不符的文档漂移)
  - 用稳定业务ID(movie_town等)建图 ✅
  - competes_with 双向关系查询 ✅（电影小镇→清明上河园/只有河南）
  - validate_graph 通过 ✅
- **发现2个技术点**：
  1. ID须用业务ID(SCENIC_SPOT_MAP)对齐，不能用随机ID
  2. homebrew python3 无 yaml 需装(实际已有)，引擎运行需有yaml的python

### 待办
- M3: 迁移生产数据(.profile/ontology 100+条)到图谱，保留SQLite
- M4-M6: 见 ADR 路线图

## ✅ Ontology 工程 M1-M4 达成（13:52-14:10，站长批准继续）
**交付物**（wiki/技术配置/Ontology架构设计/）：
1. `ADR-001-ontology-架构融合路径.md` — 架构决策
2. `movie-town-schema.yaml` v0.1.1 — 领域 schema（5+1 type，6 relation，2 constraint）
3. 新增 `运行机制-生命周期治理.md` — 何时更新/产出/优化（防再停滞）
4. `scripts/ontology_migrate.py` — 生产库→图谱迁移脚本（幂等）
5. `graph/graph.jsonl` — 图谱数据（61实体/64关系）

**验证通过**：
- ✅ 61 实体 + 64 关系，validate "Graph is valid"
- ✅ 业务查询：电影小镇竞品→只有河南/清园/银基/万岁山/方特 | 区位→中牟 | 指标→7/22 search 3626
- ✅ 修复：schema 加 Region type；迁移脚本 emit_rel 幂等化；发现并修复 3 处重复关系

**发现**：4 个 adapter（douyin/visitors/xiaohongshu/seed_content_assets）已通过 OntologyStore 写 SQLite —— M6 接入图谱有现成钩子

### 待办
- M4 固化：把 CLI 查询封装为可复用查询工具/脚本
- M5: LLM 智能层
- M6: adapter 写 SQLite 时同步写图谱（需谨慎，改生产脚本）
- 恢复/重建 ontology cron

## ✅ Ontology M4+M5+M6 达成（14:10-14:25，站长指示先做再分析）
**M4 关系查询层** `scripts/ontology_graph_query.py`
- competes/located(含同区兄弟景区)/metrics/all 四种查询
- 独特价值：located 反查出"中牟同区兄弟"（只有河南/银基）——图谱关系层才有

**M5 LLM智能层** `scripts/ontology_intent.py`
- 自然语言→图谱查询（规则翻译器，预留LLM槽位）
- 实测：竞品/区域列举/指标/全景 全通
- 修bug：区域列举意图(region_list)优先匹配"中牟有哪些景区"

**M6 接入流水** `scripts/ontology_graph_sync.py` + 钩子
- 在 ingest_objects 成功写SQLite后同步图谱（FAIL-OPEN，不影响主流程）
- 修bug：_WORKSPACE 少一层 → GRAPH_PATH 指向 ~/.openclaw/wiki
- 幂等验证：重复同步 synced=0
- 全链路：采集→SQLite→图谱→NL查询 打通

**图谱现状**：62实体(9景区/17指标/28内容/5客群/3区位)+55关系，validate通过
- M6测试写入 metric_movie_town_2026-08-10_search_index=2999（真实演示数据）

### 待站长决定（下一步分析）
- 是否保留 M6 测试写入的数据（或清理）
- ontology cron 恢复/重建
- M5 是否升级为真LLM翻译（当前规则版）
- 价值验证：竞品周报是否开始用图谱查询

## ✅ 图谱接入竞品周报 cron（14:25，站长指示"加入进去"）
- 更新 `周度竞争格局报告` cron (31080d06) payload：
  - 新增「第二步: Ontology 图谱快照」：先跑 competes/located/metrics 三个图谱查询建立关系基线
  - 第四步洞察：图谱基线 × web变化 交叉比对
  - 第六步：可选回写 sync_to_graph 保持图谱健康（无新增空执行）
- 修 2 个坑：① cron 上次运行失败是 MiniMax-M2.7 旧模型残留（现已统一 deepseek，8/17 不再有）；② payload 里 ~ 不展开 → 全改绝对路径
- 验证：3个图谱查询命令 + 回写命令在默认 python3 环境全部可执行

### 待办
- 8/17(周日) 10:00 首次验证图谱增强版周报
- ontology cron 恢复/重建（当前2个disabled）
- M6测试数据 metric_movie_town_2026-08-10_search_index=2999 是否保留待定

## ✅ 图谱自检cron + 架构打通核查（14:56，站长问"是否完美闭环"）
**新增**：`Ontology图谱自检` cron (c81ea2fa, 周一09:00, isolated) + `scripts/ontology_healthcheck.py`
- 检查：实体/关系数、距上次写入天数(停滞>7提示/>14告警)、validate、规模过小
- 输出：健康静默；warning发提醒；critical发告警（遵守发卡铁律0-1张）
- 修validate签名：validate_graph(graph_path, schema_path) 传路径非对象；yaml缺失优雅降级

**git备份链核查结果（重要！）**：
✅ workspace/ 本身是git仓库(remote=github Scenic-Area-Marketing-CN)，recent commits "vault backup"
✅ ontology全部文件已提交+push (02f4d03)，graph.jsonl被追踪，生产db被gitignore(可重建)
⚠️ 但发现架构隐患（见汇报）：
- obsidian打开的是 `~/.openclaw`（整个目录）而非 workspace/wiki，内部含多个wiki副本
- 存在 3 处 wiki 副本：workspace/wiki(主,git) / Scenic-Area-Marketing-CN(旧git仓库,不同inode) / Downloads/Scenic-Area-Marketing-CN-repo(解压副本) —— 未硬链接，主wiki和CN仓库是不同文件
- memory 和 TopLevel 用 md 在 memory/，与wiki/分离，靠AGENTS.md引导+git统一备份

### 待办
- 8/17周日10:00 图谱增强版竞品周报首次运行
- 周一09:00 图谱自检首次运行
- 架构隐患处理建议（见汇报，待站长定）

## 🚨 全面打通核查——发现核心架构分裂（14:05，站长要求"全面打通线上线下闭环"）
**牺牲品：图谱只连了抖音，线下客流/小红书割裂在外**

### 双store双库分裂（核心问题）
| Store | 位置 | 数据 | 谁写 | M6图谱 |
|---|---|---|---|---|
| 生产库 | workspace/.profile/ontology/ontology_store.db | metric101/content28/scenic9 | adapter-douyin | ✅ |
| 旧库 | ~/.profile/ontology/store.db | **metric424**/content24/scenic8/rels0 | visitors/小红书/seed | ❌ |
| 壳 | scripts/ontology/ontology_store.db | 0字节 | — | — |

- adapter-douyin 显式 import ontology/子目录store → 走ingest_objects → M6生效
- adapter-visitors/xiaohongshu/seed 走顶层ontology_store.py(旧版) → ingest_metric_snapshots/content_assets → 不走M6
- **历史数据大头(metric 424)在旧库；客流(线下核心)在本家库；生产库只有抖音**
- 结果：线下客流+小红书内容不进图谱 → 闭环断裂

### 已做安全备份
- .profile/ontology/backups_20260810_140151/ 含 old_home.db(466KB)+prod_workspace.db(253KB)+ontology_scripts.db(192KB)

### 打通方案（待站长确认）
见汇报：P1统一store→P2全adapter接M6→P3迁移合并→P4图谱回流客流

### 补充：自检cron已建(c81ea2fa周一09:00)；图谱M4-M6+周报接入已提交02f4d03

## ✅ 全面打通 + 自我进化引擎（15:20，站长确认"分别执行+自我进化机制"）

### P1 双库打通（线下闭环）✅
- 新建 scripts/ontology_merge_databases.py（FAIL-SAFE幂等合并）
- 本家库 csv客流340(visitors170+revenue170) 合并进生产库：metric 101→465
- 图谱同步：实体 62→402，关系 55→395，movie_town has_metric 343(csv线下数据)
- 线下闭环打通核心：客流+营收进生产库+图谱关系层（已验证）
- 提交 1e46e74

### P2 自我进化机制 ✅
- 新建 scripts/evolution_engine.py（结论索引三态/待验证×证据/矛盾扫描）
- 结论索引现状：已验证754 / 待验证122 / 已推翻187
- 新增 cron 824a26e3「知识进化引擎_每周自学习」(周日11:00 isolated)：
  跑engine→LLM裁决待验证→改结论索引状态→提炼决策规则→发进化卡(0-1张)
- 提交 e80353e

### 新增cron汇总
- c81ea2fa Ontology图谱自检(周一09:00)
- 824a26e3 知识进化引擎(周日11:00)
- 31080d06 周度竞品报告已接图谱快照

### 待办
- 8/17 周日: 周报(10:00)+进化引擎(11:00)首次联跑
- 8/24 周一: 图谱自检首次验证
- 顶层store(adapter-visitors等)改走新版store的ingest_objects(M6) — 已用迁移补数据,adapter本身后续可统一

### P2 适配器统一生产库（14:11-14:40）✅
- adapter-visitors.py / adapter-xiaohongshu.py：默认改走新版 ontology.ontology_store.ingest_objects（生产库+M6图谱钩子），弃用旧顶层store(本家库~/.profile)
- 客流补到最新：csv 340→424（新CSV v17，截止08-02；08-03后CSV为0/空被adapter正确跳过）
- **发现并修 bug**：sync_to_graph 关系生成用 obj.get("spot_id")，但 adapter 传 scenicSpotId → M6钩子生成的metric实体无has_metric关系。修复兼容两者，补452条缺失关系
- 图谱：实体486 关系507 | movie_town has_metric 430
- 清理手动验证的 metric_ 格式重复实体340个 + 悬空关系
- 提交 0b3645f
- ⚠️ 教训：图谱幂等依赖id唯一，不同写入方id格式必须一致（ms_<md5>为准）；adapter字段用scenicSpotId，graph_sync必须兼容

### 端到端演示 + 全面系统检查（14:13-15:00）✅
**端到端演示成功**：生产库一次查到 线下(客流4419/08-02, 营收¥34.3万) + 线上(抖音指数2999/08-10) + 竞品对比，全链路通。

**全面检查发现（按严重度）**:
1. ⚠️ 竞品 douyin 采集断档：7家竞品douyin数据全停07-22（movie_town自家08-10）— 非adapter bug(已验证resolve+转换都正常)，是采集成功率低(脚本常被限流/失败)。抖音指数日报cron a23a8099 8/10错误(gateway restart)。数据本身不连续(08-10/07-22/07-06/06-04/05-29) → 采集end问题,非代码bug
2. ✅ 清明上河园 search_index 32万非异常：是景区量级真实差异(全国知名景区vs电影小镇3千)，对比验证通过
3. ℹ️ 景区表8个重复id：仅4个有数据(fangte/haichang/wansui_mountain/yinji)，其余4个(fangte_joy/wansuishan/yinji_animal_kingdom/海昌海洋公园)是seed空壳，无数据分裂，仅图谱有冗余实体 → 低优先级不动
4. ✅ adapter统一后验证：xiaohongshu ingest_objects兼容(platform字段映射OK)、douyin竞品转换OK、visitors跑通
5. ✅ 图谱一致性：实体486关系507，自检healthy
6. ⚠️ cron大量rate-limited(FailoverError) + 少量超时(model-call-started)：外部API配额/响应问题，非代码bug

**结论**：代码层无致命bug。主要风险=抖音采集成功率低(竞品断档)+deepseek配额。已记录，不修代码。

## 14:21 全系统健康检查（08-10）
**执行人**：李涯

### ✅ 正常项
- Gateway 运行中（uptime 1m8s，端口18789）
- 模型 deepseek/deepseek-v4-flash，余额 ¥48.02，api-key 正常
- 38 个 cron 任务模型全部已切 deepseek（无遗漏 MiniMax）
- CDP 18800 Chrome/151 正常，7 个 Tab
- 代理 7897 可达
- 磁盘 20% 用量，空间充足
- 抖音/小红书采集脚本 + send_feishu_card.py 存在

### 🔧 已修复
- **Cookie 文件缺失**：Mac 重启清空 /tmp（juLiang + xiaohongshu 均消失）→ 用 cdp_cookie_hub.py 从 CDP 浏览器重新同步：
  - 抖音 60 条、小红书 17 条、微博 24 条
  - cookie_health_check 复检全绿（douyin 58 / xiaohongshu 17，均非登录页）
- **周度竞争格局报告**：已确认配置为 deepseek，依赖脚本 ontology_graph_query.py + ontology_graph_sync.py 均存在（8/10 下午刚更新）

### ⚠️ 观察（非问题）
- 大量 cron last=error 均为【8/10 切换前 MiniMax rate-limited】或【网关重启瞬时中断】的历史错误，非 deepseek 配置问题；deepseek 下次自然运行即可验证
- config validate：2 个 warning（pixverse env-var 弃用提醒 + memory-core plugin disabled）—— 均为无害，待后续处理

## 14:22-14:27 清理 cron 错误 + 删除无用任务（站长指令"处理所有error，删除无用"）
**执行人**：李涯

### 🗑️ 已删除 6 个无用 disabled 任务（均已被合并/弃用/断档）
| 任务 | 删除原因 |
|------|----------|
| 小红书日报 (8a4c1b5d) | 站长决策不修（断档49天） |
| 系统进化审视 (6bb42dd4) | 已被周日洞察汇总合并 |
| Wiki健康检查 (ca0bc5c3) | 已被周日洞察汇总合并 |
| 文旅情报日报 (12d069e0) | 功能并入竞品内容动态+舆情监控 |
| ontology_daily_work (01cc75a7) | 脚本缺失，新流程ontology_healthcheck替代 |
| Ontology架构研究_每周深化 (bf5a6217) | Ontology工程新流程替代 |

### 🔧 CDP Cookie同步 根治（agentTurn→command）
- 原问题：agentTurn 走 LLM 导致 model-call-started 超时（consErr=5）
- 修复：改用 `openclaw cron edit --command` 直接跑 cdp_cookie_hub.py（纯脚本，不耗token，timeout 120s）
- 效果：消除 LLM 超时根因，token 0 消耗

### 📊 现状
- cron 总数 37→31（删6）
- 20 个 enabled 任务 last=error 均为【MiniMax时代 rate-limited + gateway restart】历史瞬时错误
- 全部已配置 deepseek，下次自然运行时自动清除 error 状态（不 force run，避免浪费token）
